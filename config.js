// config.example.js — committed template. Copy to config.js and fill in real values.
// config.js is gitignored and must never be committed.
var CADE_CONFIG = {
  firebase: {
    databaseURL: "https://aria-training-default-rtdb.firebaseio.com",
    projectId: "aria-training"
  },
  model: "claude-haiku-4-5-20251001",
  userPath: "brandon",
  proxyUrl: "https://cade-proxy.YOUR-SUBDOMAIN.workers.dev"
};
